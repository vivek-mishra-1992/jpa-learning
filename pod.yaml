---
    apiVersion: v1
    kind: Pod
    metadata:
      name: jpalearning
      labels:
        app: jpalearning
    spec:
       containers:
       - name: jpalearningcontainer
         image: vivek/jpalearning
         imagePullPolicy: Never
         ports:
          - containerPort: 8080
...